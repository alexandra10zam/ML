# Dataset visualizations

Run the visualization script to generate plots into the `figures/` folder.

Usage:

```bash
python visualize.py
```

Install dependencies:

```bash
pip install -r requirements.txt
```

Output: PNG files and `summary_stats.csv` in `figures/`.
